name: Paper

# Disabled stub retained so historical workflow links do not 404.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Manuscript build disabled
        run: |
          echo "Manuscript build is disabled in this workflow."
          exit 0
