# Recorded interpreter environment for the v0.1.0 release run.
# Authoritative, resolvable dependency pins live in `uv.lock`; install with
# `uv sync --all-extras`. This file documents the exact versions used to
# produce the published values and is not a substitute for the lockfile.
# Every entry below is declared in `pyproject.toml`.
jsonschema==4.26.0
matplotlib==3.11.1
numpy==2.3.5
pandas==2.2.3
PyYAML==6.0.3
typer==0.26.3
pytest==9.1.1
coverage==7.13.3
